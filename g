# elif pygame.mouse.get_pressed(num_buttons=3)[0] and pygame.mouse.get_pos()[0] >= 390:
    #     c = 390
    #     # pygame.draw.rect(screen, (255, 0, 0), pygame.Rect(x, y, 390 - x, 20), 0, 10)
    #     # pygame.draw.circle(screen, (0, 0, 0), (390, y + 10), 15)
    #     # return (100)
    # elif pygame.mouse.get_pressed(num_buttons=3)[0] and pygame.mouse.get_pos()[0] <= 210:
    #     c = 210
        # pygame.draw.circle(screen, (0, 0, 0), (210, y + 10), 15)
        # return 0